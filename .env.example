# Credentials from https://my.telegram.org/apps
APP_ID=123456
APP_HASH=0123456789abcdef0123456789abcdef

# Phone number in international format.
TG_PHONE=+10000000000

# Optional: where to store the session (default: ./session).
# TG_SESSION_DIR=session

# Optional: mark messages as sent by an agent.
# "footer" appends an italic line; "bot" routes messages through the inline
# echo bot so Telegram renders a "via @bot" header (needs `tgmcp echobot`).
# TG_ATTRIBUTION=footer
# TG_AGENT_FOOTER=sent by an agent
# TG_BOT_TOKEN=123456:AA...
# TG_ATTRIBUTION_STRICT=true

# Optional: serve downloaded files out of band instead of returning bytes, so
# large files never enter the model's context.
#
# Either this process serves them, in which case the URL must be reachable by
# the client...
# TG_BLOB_BASE_URL=http://127.0.0.1:8080/blob
# TG_BLOB_TTL=15m
#
# ...or a bucket does, which also works when the client cannot reach this
# process. Credentials come from the usual AWS/MinIO environment variables, not
# from here. Set a lifecycle rule: tgmcp does not delete stored objects.
# TG_BLOB_S3_ENDPOINT=s3.example.com
# TG_BLOB_S3_BUCKET=blobs
# TG_BLOB_S3_PREFIX=tenants/alice
# TG_BLOB_S3_REGION=

# Optional: log MCP request and response bodies at debug level. That is the
# contents of your chats, and it reaches every log exporter. Debugging only.
# TG_LOG_PAYLOADS=true

# Optional: telemetry. Exporters default to OTLP over gRPC to localhost:4317;
# set them to "none" to run without a collector.
# OTEL_TRACES_EXPORTER=none
# OTEL_METRICS_EXPORTER=none
# OTEL_LOGS_EXPORTER=none
